#include <stdio.h>
/**
* main -Entry point
* Program that prints a string using printf function
* Return: Always 0 (Success)
*/
int main(void)
{
	a = 0;
	while (a < b)
	{
	printf("%d\n", a);
	a++;
	}
}
