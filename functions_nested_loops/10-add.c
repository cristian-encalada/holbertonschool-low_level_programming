#include "main.h"
/**
* add - function that adds two integers and returns the result
* @a: integer value 1
* @b: integer value 2
* Return: 0 at the end if no errors
*/
int add(int a, int b)
{
	int sum = 0;

	sum = (a + b);
	return (sum);
}
